# Grova

This project can run **without Replit**. See [README.md](./README.md) for local development and deployment.

## Grova app only

```bash
pnpm install
pnpm dev:grova      # development
pnpm build:grova    # production build
pnpm start:grova    # production server (UI + API on port 5000)
```
